import React from "react";

export default function CustomBotTestPreview({ name, persona, knowledge }: { name: string; persona: string; knowledge: string }) {
  // Removed test input and preview

  return null;
}